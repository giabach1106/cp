#include <iostream>
using namespace std;
int main(){
	int n,a[1000];
	cin >> n;
	for(int i=0;i<n;i++)
		cin >> a[i];

	for(int i=0;i<n;i++){
		int max= a[0]
		if(max < a[i])
			max = a[i];
	}
	for(int i=1;i<n;i++){
		int min= a[0]
		if(min > a[i])
			min= a[i];
	cout << max<< endl<< min;


	return 0;
}